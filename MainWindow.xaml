<Window x:Class="FlightDetector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlightDetector"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1500">


    <Grid Margin="0,-3,0,3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <local:dataDisplayView Grid.Column="0" DataContext="{Binding Path=DataDisplayViewModel}" Margin="430,10,-430,-85" Grid.RowSpan="2" Grid.ColumnSpan="3" Grid.Row="1"/>
        <local:joystick Grid.Row="0" Grid.Column="0" DataContext="{Binding Path=JoystickViewModel}"></local:joystick>
        <local:FooterView Grid.Column="0" DataContext="{Binding Path=FooterViewModel}" Grid.ColumnSpan="4" Margin="750,39,-750,58"/>
    </Grid>

</Window>
