<Page x:Class="FlightDetector.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FlightDetector"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Path=ValidFlight}"></TextBlock>
        <TextBlock Grid.Row="1" Text="{Binding Path=FlightToDetect}"></TextBlock>
        <TextBlock Grid.Row="2" Text="{Binding Path=DllPath}"></TextBlock>
        <TextBlock Grid.Row="3" Text="{Binding Path=DetectorType}"></TextBlock>
    </Grid>
</Page>

    
    